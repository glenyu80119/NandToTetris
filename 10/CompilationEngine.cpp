#include<CompilationEngine.h>
#include<iostream>
#include<fstream>

CompilationEngine::CompilationEngine(string input, string output) {
	ifstream(input);
	ofstream(output);
}

CompilationEngine::CompileClass(){
	
}

CompilationEngine::eat(string token) {
	if (token != )
}